# Supabase (optional — app runs in demo mode if empty)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# AI providers — NEVER expose secrets in the frontend.
# These are used by a serverless layer / backend only.
VITE_AI_PROVIDER=demo
# VITE_TEXT_TO_VIDEO_ENDPOINT=/api/ai/text-to-video
# VITE_AVATAR_GENERATION_ENDPOINT=/api/ai/avatar